name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  doctor_soft_angola_api:
    path: ../../doctorsoft_angola_api

dev_dependencies:
  very_good_analysis: ^3.0.1
